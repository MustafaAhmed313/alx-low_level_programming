#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print alphabet consecutive representaion
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int n;

	for (n = 48 ; n <= 57 ; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
